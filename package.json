{
  "name": "open-hoops",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first basketball video analytics platform.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "generate-types": "python packages/shared_types/generate_types.py"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
